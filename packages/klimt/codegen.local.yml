overwrite: true
schema: ./src/server/resolvers/schema.graphql
documents:
    - '**/*.local.graphql'
generates:
    app/[locale]/_layout/generated/local-codegen.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - 'typed-document-node'
        config:
            exportFragmentSpreadSubTypes: true
            documentMode: graphQLTag
            skipTypename: true
            useTypeImports: true
hooks:
    afterAllFileWrite:
        - prettier --write
