overwrite: true
schema: ./src/resolvers/schema.graphql
generates:
    src/resolvers/resolvers.types.ts:
        plugins:
        - typescript
        - typescript-resolvers
        config:
            useIndexSignature: true
hooks:
    afterAllFileWrite:
        - prettier --write
